register.html

novalidate="novalidate"  
当提交表单时不对表单数据（输入）进行验证
这是表单的一个新的属性，用来阻止提交表单前的验证。
那么，这个有什么作用呢？
我个人认为，是在自己编写表单验证插件的时候（如jquery.validate），为了避免和默认的表单验证冲突，建议加上这个属性。


地区里 <input type="hidden" id="areaId" name="">  ?用来干啥
地区要用jquery写完 地级市 县区

autocomplete="off"
autocomplete 属性规定输入字段是否应该启用自动完成功能。
自动完成允许浏览器预测对字段的输入。当用户在字段开始键入时，浏览器基于之前键入过的值，应该显示出在字段中填写的选项。

主页 注册 商品列表 商品详情 购物车 (404)

